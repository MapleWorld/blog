<div class="comment">
 	<strong><%=h comment.commenter %> says:</strong><br />
 	<%=h comment.body %>
  
 	<% if session[:current_user_id] ||= nil %><br />
		<%= link_to "Delete", comment, :confirm => "Are you sure?", :method => :delete%>
	<% end %>
</div>
